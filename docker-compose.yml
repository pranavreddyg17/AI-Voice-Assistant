# Optional: Run Qdrant with persistent storage for production
# For hackathon, the backend uses in-memory Qdrant by default
version: '3'
services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage
volumes:
  qdrant_storage:
